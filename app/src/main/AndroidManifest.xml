<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.deeksha.mycheck">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/pandi2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main2" />
        <activity android:name=".FrenchPhrases" />
        <activity android:name=".FrenchOptions" />
        <activity android:name=".FrenchBasics" />
        <activity android:name=".FrenchAnimals" />
        <activity android:name=".FrenchColors" />
        <activity android:name=".FrenchFood"></activity>
    </application>

</manifest>